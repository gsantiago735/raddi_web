import 'package:flutter/material.dart';

class GenericStatusDialog extends StatelessWidget {
  const GenericStatusDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Dialog(
      child: Text("Error"),
    );
  }
}
